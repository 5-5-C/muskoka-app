<div class="row">
<%= form_for(@user) do |f| %>

  <% if @user.errors.any? %>
    <div class="error_messages">
      <h2>Form is invalid</h2>
      <ul>
        <% @user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="col-lg-5 email-form">
    <div class="field">
      <%= f.text_field :email, placeholder: "Email", :class => "email" %>
    </div>
  </div>
  <div class="col-lg-2 col-sm-4 submit-form">
    <div class="main-page">
      <%= f.submit 'Submit' %>
      <% end %>
    </div>
  </div>
  <div class="col-lg-5 col-sm-8 facebook-login">
    <a class="btn btn-block btn-social btn-facebook" href="/auth/facebook">
      <span class="fa fa-facebook"></span> Or Sign up with Facebook
    </a>
  </div>
</div>
